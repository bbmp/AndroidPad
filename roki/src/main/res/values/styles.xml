<resources>

    <!-- 对话框样式 -->
    <style name="roki_dialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>

    </style>

    <style name="roki_li_dialog_default" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/roki_dialog_default_show</item>  <!--显示动画-->
        <item name="android:windowExitAnimation">@anim/roki_dialog_default_cancel</item> <!--消失动画-->
    </style>
</resources>